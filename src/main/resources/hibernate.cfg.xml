<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
    "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
    "http://hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
  <session-factory>
    <!-- SQLite Database Connection -->
    <property name="hibernate.connection.driver_class">org.sqlite.JDBC</property>
    <property name="hibernate.connection.url">jdbc:sqlite:db/data.db</property>

    <!-- SQLite Dialect -->
    <property name="hibernate.dialect">org.hibernate.community.dialect.SQLiteDialect</property>

    <!-- Auto-create/update tables based on entities -->
    <property name="hibernate.hbm2ddl.auto">update</property>

    <!-- SQL logging disabled for clean CLI output -->
    <property name="hibernate.show_sql">false</property>
    <property name="hibernate.format_sql">false</property>

    <!-- Entity mappings -->
    <mapping class="edu.ntnu.idi.idatt.model.Author"/>
    <mapping class="edu.ntnu.idi.idatt.model.DiaryEntry"/>
  </session-factory>
</hibernate-configuration>